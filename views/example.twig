<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>
   <!-- ~ alt+n tilde : sert à remplacer les parenthèses-->
    {% for key, item in values %}
        <div>{{ key ~ ' = ' ~ item }}</div>
    {% endfor %}

    {% for index in range(1, 10, 1) %}
        <a href="page/{{ index }}">{{ index }}</a>
    {% endfor %}

    {% set toto = 'coucou' %}
    {{ toto }}
</body>
</body>
</html>